<?xml version="1.0"?>
<object name="ftw.noticeboard.Category"
        meta_type="Dexterity FTI">

    <!-- Basic metadata -->
    <property name="title">Category</property>
    <property name="icon_expr"></property>
    <property name="allow_discussion">False</property>
    <property name="global_allow">False</property>
    <property name="filter_content_types">True</property>

    <!-- schema interface -->
    <property name="schema">ftw.noticeboard.types.category.ICategorySchema</property>

    <!-- class used for content items -->
    <property name="klass">ftw.noticeboard.types.category.Category</property>

    <property name="add_permission">ftw.noticeboard.AddCategory</property>

    <property name="behaviors">
        <element value="plone.app.dexterity.behaviors.metadata.IBasic"/>
    </property>

    <!-- Method aliases -->
    <alias from="(Default)" to="(dynamic view)"/>
    <alias from="edit" to="@@edit"/>
    <alias from="sharing" to="@@sharing"/>
    <alias from="view" to="(selected layout)"/>

    <!-- Actions -->
    <action
        action_id="view"
        title="View"
        category="object"
        condition_expr=""
        url_expr="string:${object_url}"
        visible="True">
        <permission value="View"/>
    </action>

    <action
        action_id="edit"
        title="Edit"
        category="object"
        condition_expr=""
        url_expr="string:${object_url}/edit"
        visible="True">
        <permission value="Modify portal content"/>
    </action>

</object>
